<div class="board">
    <%= for %{"category" => category, "clues" => clues} <- @round do %>
        <div class="category">
            <div class="header">
                <%= raw(category) %>
            </div>

            <%= for clue <- clues do %>

                <%= live_patch to: Routes.game_edit_double_jeopardy_path(@socket, :edit_clue, @game, Map.get(clue, "id")), class: "clue" do %>
                <span class="value">$<%= Map.get(clue, "value") %></span>
                <!--
                <p class="clue_text">Q: <%= Map.get(clue, "clue") %></p>
                <p class="answer_text">A: <%= Map.get(clue, "answer") %></p>
                -->
                <% end %>

            <% end %>
        </div>
    <% end %>
</div>
